# 视频裁剪功能指南

## 📖 功能说明

新增两个参数用于裁剪视频：
- **`video_start_frame`**: 从第几帧开始处理
- **`video_end_frame_offset`**: 从结尾倒数第几帧结束

这两个参数会同时作用于**视频帧提取**和**音频提取**，保持音视频同步。

## 🎯 配置参数

### video_start_frame
从视频的第几帧开始处理（0表示从开头）

```yaml
video_start_frame: 0    # 从开头开始（默认）
video_start_frame: 30   # 跳过前30帧
video_start_frame: 90   # 跳过前90帧（30fps视频约3秒）
```

### video_end_frame_offset  
从视频结尾倒数第几帧结束（0表示处理到结尾）

```yaml
video_end_frame_offset: 0    # 处理到结尾（默认）
video_end_frame_offset: 30   # 忽略最后30帧
video_end_frame_offset: 90   # 忽略最后90帧（30fps视频约3秒）
```

## 📊 使用示例

### 示例 1: 完整视频（默认）
```yaml
video_start_frame: 0
video_end_frame_offset: 0
```
**效果**: 处理整个视频

---

### 示例 2: 跳过开头
```yaml
video_start_frame: 90      # 跳过前3秒（假设30fps）
video_end_frame_offset: 0
```
**适用场景**: 
- 视频开头有无关内容（如标题、准备阶段）
- 想跳过前几秒的过渡画面

**效果**:
- 30fps 视频: 跳过前 3 秒
- 60fps 视频: 跳过前 1.5 秒

---

### 示例 3: 忽略结尾
```yaml
video_start_frame: 0
video_end_frame_offset: 60    # 忽略最后2秒（假设30fps）
```
**适用场景**:
- 视频结尾有无关内容（如结束画面、片尾）
- 活动已经结束，后面只是收尾

**效果**:
- 30fps 视频: 忽略最后 2 秒
- 60fps 视频: 忽略最后 1 秒

---

### 示例 4: 提取中间片段
```yaml
video_start_frame: 90       # 跳过前3秒
video_end_frame_offset: 60  # 忽略最后2秒
```
**适用场景**:
- 只需要分析核心活动部分
- 去除开头和结尾的无关内容

**效果**: 
- 原视频 30秒 (900帧 @ 30fps)
- 处理范围: 第90帧 到 第840帧
- 实际处理: 25秒 (750帧)

---

### 示例 5: 精确时间段（配合帧率计算）

假设原视频是 **30fps**，想提取 **第5秒 到 第20秒**：

```yaml
# 第5秒 = 5 × 30 = 150帧
# 第20秒 = 20 × 30 = 600帧
# 视频总长30秒 = 30 × 30 = 900帧
# 结尾offset = 900 - 600 = 300帧

video_start_frame: 150      # 从第5秒开始
video_end_frame_offset: 300 # 到第20秒结束（忽略最后10秒）
```

## 🧮 帧数计算公式

### 时间转帧数
```
帧数 = 时间(秒) × 视频帧率(fps)
```

**示例**:
- 30fps 视频，5秒 = 5 × 30 = **150帧**
- 60fps 视频，3秒 = 3 × 60 = **180帧**

### 结尾offset计算
```
video_end_frame_offset = 总帧数 - 结束帧位置
```

**示例**:
- 总帧数900帧，想在第750帧结束
- offset = 900 - 750 = **150**

## 📝 实际场景应用

### 场景 1: 儿童画画视频
**原始视频**: 30秒，前5秒在准备画具，最后3秒在收拾

```yaml
video_fps: 1
video_start_frame: 150      # 跳过前5秒 (5×30fps)
video_end_frame_offset: 90  # 忽略最后3秒 (3×30fps)
```

**结果**: 只分析中间22秒的实际画画过程

---

### 场景 2: 快速测试
**原始视频**: 5分钟，只想快速看前30秒效果

```yaml
video_fps: 1
video_start_frame: 0
video_end_frame_offset: 8100  # 忽略后4分30秒 (270秒×30fps)
```

**结果**: 只处理前30秒，节省时间和成本

---

### 场景 3: 多段视频拼接
如果有一个长视频，想分段分析：

**第一段** (0-30秒):
```yaml
video_start_frame: 0
video_end_frame_offset: 8100  # 900帧 - 270秒×30fps
```

**第二段** (30-60秒):
```yaml
video_start_frame: 900        # 30秒×30fps
video_end_frame_offset: 7200  # 忽略60秒后的内容
```

## ⚠️ 注意事项

### 1. 帧数验证
程序会自动验证参数：
- 如果 `start_frame < 0`，自动重置为 0
- 如果 `start_frame >= 总帧数`，自动重置为 0
- 如果 裁剪后无有效帧，将使用完整视频

### 2. 音视频同步
音频会自动按照视频裁剪范围同步裁剪，确保音视频一致。

### 3. 日志输出
运行时会显示详细的裁剪信息：
```
视频信息: 30.00 FPS, 900 帧, 30.00 秒
裁剪范围: 第 150 帧 到 第 810 帧 (共 660 帧, 22.00 秒)
跳过: 开头 150 帧 (5.00秒), 结尾 90 帧 (3.00秒)
```

### 4. 性能优化
裁剪可以显著减少处理时间和API成本：
- 原视频 5分钟 (9000帧) → 裁剪到 1分钟 (1800帧)
- 成本降低约 **80%**

## 🔧 快速配置

编辑 `config.yml`:

```yaml
# 视频提取配置
video_fps: 1
video_max_width: 640
video_start_frame: 0      # 修改此值：从第几帧开始
video_end_frame_offset: 0 # 修改此值：从结尾倒数第几帧结束
```

## 💡 最佳实践

1. **先预览视频**: 用视频播放器查看，确定需要裁剪的部分
2. **计算帧数**: 用公式 `帧数 = 秒数 × fps` 计算
3. **小范围测试**: 先用小范围测试效果
4. **查看日志**: 确认实际裁剪范围符合预期
