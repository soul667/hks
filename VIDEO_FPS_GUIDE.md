# 视频帧率配置指南 (video_fps)

## 📖 配置说明

`video_fps` 参数支持两种灵活的配置方式：

### 方式 1: 每秒提取几帧 (video_fps >= 1)

当 `video_fps >= 1` 时，表示**每秒提取的帧数**。

**示例：**
```yaml
video_fps: 1    # 每秒提取 1 帧
video_fps: 2    # 每秒提取 2 帧
video_fps: 5    # 每秒提取 5 帧
video_fps: 10   # 每秒提取 10 帧
```

**适用场景：**
- 需要较高时间精度的分析
- 动作变化较快的视频
- 需要捕捉更多细节

### 方式 2: 每 N 秒提取 1 帧 (video_fps < 1)

当 `video_fps < 1` 时，表示**帧率的倒数**，即每隔多少秒提取一帧。

**示例：**
```yaml
video_fps: 0.5    # 每 2 秒提取 1 帧 (1/0.5 = 2)
video_fps: 0.333  # 每 3 秒提取 1 帧 (1/0.333 ≈ 3)
video_fps: 0.25   # 每 4 秒提取 1 帧 (1/0.25 = 4)
video_fps: 0.2    # 每 5 秒提取 1 帧 (1/0.2 = 5)
video_fps: 0.1    # 每 10 秒提取 1 帧 (1/0.1 = 10)
```

**适用场景：**
- 视频较长，需要降低成本
- 场景变化缓慢
- 只需要了解大致内容

## 📊 配置对比

| video_fps | 含义 | 10秒视频提取帧数 | 60秒视频提取帧数 | 适用场景 |
|-----------|------|------------------|------------------|----------|
| `10` | 每秒10帧 | 100帧 | 600帧 | 高精度分析、快速动作 |
| `5` | 每秒5帧 | 50帧 | 300帧 | 详细分析 |
| `2` | 每秒2帧 | 20帧 | 120帧 | 中等精度 |
| `1` | 每秒1帧 | 10帧 | 60帧 | **推荐默认值** |
| `0.5` | 每2秒1帧 | 5帧 | 30帧 | 低成本、长视频 |
| `0.333` | 每3秒1帧 | ~3帧 | 20帧 | 极低成本 |
| `0.2` | 每5秒1帧 | 2帧 | 12帧 | 超长视频概览 |

## 💡 推荐配置

### 儿童活动识别 (本项目)
```yaml
video_fps: 1      # 每秒1帧，平衡成本和精度
# 或
video_fps: 0.5    # 每2秒1帧，降低成本
```

### 其他场景
- **视频会议/讲座**: `0.2` - `0.5` (每5-2秒1帧)
- **运动分析**: `5` - `10` (每秒5-10帧)
- **监控视频**: `0.1` - `0.5` (每10-2秒1帧)
- **快速浏览**: `0.5` - `1` (每2秒-1秒1帧)

## 🎯 实际示例

### 示例 1: 画画视频 (30秒，30fps原始帧率)
```yaml
video_fps: 1
```
**结果：**
- 提取 30 帧 (每秒1帧)
- 平均每 30 帧提取 1 帧
- 适合捕捉画画的主要过程

### 示例 2: 长时间活动视频 (5分钟，30fps)
```yaml
video_fps: 0.333
```
**结果：**
- 提取约 100 帧 (每3秒1帧)
- 平均每 90 帧提取 1 帧
- 降低成本的同时保持足够的信息量

### 示例 3: 快速运动视频 (10秒，60fps)
```yaml
video_fps: 5
```
**结果：**
- 提取 50 帧 (每秒5帧)
- 平均每 12 帧提取 1 帧
- 能够捕捉快速动作的细节

## ⚠️ 注意事项

1. **成本考虑**: 帧数越多，API 调用成本越高
2. **性能影响**: 帧数越多，处理时间越长
3. **精度权衡**: 帧数太少可能遗漏关键信息
4. **建议范围**: 
   - 短视频 (< 30秒): `1` - `5`
   - 中等视频 (30秒 - 5分钟): `0.5` - `2`
   - 长视频 (> 5分钟): `0.2` - `1`

## 🔧 如何修改配置

编辑 `config.yml` 文件：

```yaml
# 视频提取配置
video_fps: 1  # 修改此值
```

然后运行程序，日志会显示实际提取的帧数和帧率信息。
